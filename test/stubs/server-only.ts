// Empty stub for `server-only` so Vitest can import modules that use the
// Next.js server-only guard. The real package throws at bundle time if used
// in client code; the stub is a no-op so tests can exercise server modules.
export {}
